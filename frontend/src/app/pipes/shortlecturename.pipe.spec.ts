import { ShortlecturenamePipe } from './shortlecturename.pipe';

describe('ShortlecturenamePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortlecturenamePipe();
    expect(pipe).toBeTruthy();
  });
});
